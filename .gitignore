# Dependencies
node_modules/
.pnpm-store/

# Build outputs
dist/
build/
.next/
.turbo/
out/

# Environment files
.env
.env.local
.env.*.local

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Logs
logs/
*.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*

# Testing
coverage/
.nyc_output/

# Generated files
*.tsbuildinfo
.eslintcache

# Docker
docker-compose.override.yml

# Prisma
prisma/*.db
prisma/*.db-journal

# PWA
public/sw.js
public/workbox-*.js
public/sw.js.map
public/workbox-*.js.map

# Private maintainer overlay repo (internal planning, not for public repo).
# Anchored to the repo root so it only ignores the top-level `ai/` repository —
# NOT public `ai/` subdirectories like `docs/ai/` or `apps/api/src/core/ai/`.
/ai/
# CLAUDE.md is now tracked (a thin @AGENTS.md importer — public, no secrets).
# The private maintainer overlay lives in CLAUDE.local.md (gitignored).
CLAUDE.local.md

# Claude Code local agent state (permissions, locks) — never commit
.claude/

# Local Prisma generated client (will be in node_modules)
generated/

# Temporary files
tmp/
temp/
*.tmp
