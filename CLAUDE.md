@AGENTS.md

## Claude Code

The full, cross-tool guide is `@AGENTS.md` above (shared with every agent — single
source of truth, no duplication). Its "Maintainer mode" section tells you to read
`ai/STATUS.md`, `ai/WORKFLOW.md`, and `ai/DECISIONS.md` **only if an `ai/`
directory is present** (the maintainer's gitignored copy). Otherwise the public
docs are complete.
