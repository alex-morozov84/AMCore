@AGENTS.md

## Claude Code

The full, cross-tool guide is `@AGENTS.md` above (shared with every agent — single
source of truth, no duplication). Its "Operating context" section routes a
maintainer checkout through the private `ai/` documentation only when that
directory is present. Otherwise the public docs are complete.
